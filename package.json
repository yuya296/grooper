{
  "name": "chrome-tab-organaizer",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "DIAGNOSTICS=1 pnpm build && playwright test",
    "e2e:headless": "PW_HEADLESS=1 DIAGNOSTICS=1 pnpm build && playwright test",
    "lint": "node scripts/lint.mjs"
  },
  "bin": {
    "tabgrouper": "dist/cli/index.js"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/chrome": "^0.0.271",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  }
}
