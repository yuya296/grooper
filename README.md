# Grooper — Rule-based tab grouping

Chrome拡張として動作する、ルールベースのタブグルーピングツールです。

## Development

```bash
pnpm install
pnpm build
pnpm test
```

生成物は `dist/` 配下に出力されます。

## Release

Chrome Web Store公開手順は `docs/chrome-web-store-release.md` を参照してください。
